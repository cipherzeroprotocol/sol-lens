# First, fetch the remote changes
git fetch origin

# Option 1: If you want to keep your changes and merge with remote
git pull origin main

# Option 2: If you want to force your version (use with caution)
# git push --force origin main

# Option 3: If you want to rebase your changes on top of remote
# git pull --rebase origin main
# git push origin main
